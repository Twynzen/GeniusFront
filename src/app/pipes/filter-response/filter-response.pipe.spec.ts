import { FilterResponsePipe } from './filter-response.pipe';

describe('FilterResponsePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResponsePipe();
    expect(pipe).toBeTruthy();
  });
});
